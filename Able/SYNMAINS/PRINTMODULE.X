module	printmodule;	dcl (i,j,k) fixed;	print 'in print module at this time... 94';	i = 99; j = 0;		while (j < 2048*3)	{		write(48) = j;		if (read(51) == 1 && read(51) == 1)			k = 1;		else			k = 0;				if (k != i)		// if different condition found		{			if (i != 0 && k == 0)				print 'start of data: ', j;			else				print 'start of ones: ', j;			i = k;		}				j = j + 1;	}		write("324") = read("333"); /* r13i to atnv to start tracing */end printmodule;