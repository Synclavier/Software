/* ROUTDCL -- $Title External declarations for Multichannel and Midi */dcl MULTICHANNEL.DISPLAY    proc(fixed) external;  /* call to activate DTD track display */dcl MIDI.DISPLAY            proc(fixed) external;  /* call to activate DTD track display */library ':syntlibs:routlib';  /* multichannel and midi */