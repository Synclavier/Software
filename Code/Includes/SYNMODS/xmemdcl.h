/* Xmemdcl -- external memory utility module */extern	array	copy_buf;						/* Buffer used by copy routines - can be used by others with care	*/extern	array	diskio_info;					/* Info used by polyread, polywrite, extread, extwrite	*/extern	void	copy_in(fixed, fixed);			/* Copy block into main memory - set up mam & mal before calling	*/extern	void	copy_out(fixed, fixed);			/* Copy block to ex mem - set up mam & mal before calling	*/extern	void	copy_ext_mem(fixed, fixed, fixed, fixed, fixed);	/* High speed copies - copies up or down, any length, in external memory	*/extern	void	copy_ext_mem_sec(fixed, fixed, fixed);	/* Sector boundaries & lengh					*/extern	void	ext_readdata(fixed, fixed, fixed, fixed, fixed, fixed);	/* Diskread directly into ext memory			*/extern	void	ext_writedata(fixed, fixed, fixed, fixed, fixed, fixed);	/* Diskwrite directly into ext memory			*/#include "xmemlits.h"_library(":SYNLIBS:XMEMLIB")					/* Access the code itself						*/