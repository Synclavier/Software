/* This library allows you to scanf or printf to a given window.  To use it you must call debug_init () with the window that is  to be used for debugging, after the text characteristics of the  window are set up.    It is the user of this library's responsibility to handle all  events for this window, including update (all you can do is  erase the update region).    This library must be linked in BEFORE CRuntime.o. */#ifndef NED__DEBUG#define NED__DEBUG#include <Windows.h>#ifdef __safe_linkextern "C" {#endifvoid debug_init (WindowPtr window);			/* initialize debugger */#ifdef __safe_link}#endif#endif	/* NED__DEBUG */