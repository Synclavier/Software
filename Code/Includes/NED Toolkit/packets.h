/* NED Toolkit - DAWN Protocol Packet Interface Definitions   Copyright © 1990-1992 by New England Digital Corporation   By:  Karim J. Chichakly on 24 May 1989*/#ifndef NED__PACKETS#define NED__PACKETS#ifndef NED__C#include "c.h"#endif#include "packetstructs.h"/* prototypes */#ifdef __cplusplusextern "C" {#endifPACKET	*new_packet (void);					/* allocate a packet of size PACKET_MAX */PACKET	*alloc_packet (int length);			/* allocate a packet of a given length (can be larger than PACKET_DATA_MAX) */void		free_packet (PACKET *packet);			/* free a packet */int		send_packet (PACKET *packet);			/* send a packet */void		print_packet (PACKET *packet);		/* print out packet */void 	get_dawn_connection_name(char *here);	// return name of session, if anyvoid	dawn_terminate_current_session();		// terminate link session if one; e.g. return to mac422#ifdef __cplusplus}#endif#endif				/* NED__PACKETS */